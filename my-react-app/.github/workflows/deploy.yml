name: Deploy React App to Linux Server

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Node
      uses: actions/setup-node@v3
      with:
        node-version: 18

    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: npm run test -- --watchAll=false

    - name: Build app
      run: npm run build

    - name: Build Docker image
      run: docker build -t my-react-app .

    - name: Copy files to server
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        source: "."
        target: "/home/${{ secrets.SERVER_USER }}/my-react-app"

    - name: Deploy Docker container on server
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        script: |
          cd ~/my-react-app
          docker build -t my-react-app .
          docker stop react_container || true
          docker rm react_container || true
          docker run -d --name react_container -p 80:80 my-react-app
